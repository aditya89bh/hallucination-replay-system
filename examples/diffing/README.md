# Diffing Examples

Deterministic Phase 8 examples for execution comparison:

- `successful_vs_failed.json` compares a successful tool execution with a failed timeout.
- `retrieval_regression.json` shows retrieved evidence disappearing between runs.
- `memory_regression.json` shows memory state changing between runs.
